{
    "builds": [
        {
            "srs": "app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "srs": "/(.*)",
            "dest": "/app.py"
        }
    ]
}